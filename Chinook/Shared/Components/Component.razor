@foreach (var item in UserPlaylistViewModel.PlayLists)
    {    
    var urlPlaylist = string.Concat("playlist/", item.PlaylistId);
    <NavLink class="nav-link" href="@urlPlaylist" @onclick="HandleClick">
        <i class="bi bi-music-note-list me-3" style="font-size: 1.25em"></i>@item.Name
    </NavLink>
    }


@inject NavigationManager _navigationManager

@code {
    [Parameter]
    public ViewModels.UserPlaylistViewModel UserPlaylistViewModel { get; set; }

    [Parameter]
    public EventCallback<ViewModels.UserPlaylistViewModel> UserPlaylistViewModelChanged { get; set; }

    void HandleClick()
    {
        //_navigationManager.NavigateTo("editemployee", true);
    }
}